apiVersion: v1
kind: Service
metadata:
  name: {{ include "banzai-web-frontend.fullname" . }}
  labels:
    {{- include "banzai-web-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "banzai-web-frontend.selectorLabels" . | nindent 4 }}
