apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ./ing/

configMapGenerator:
  - name: app-configuration
    behavior: replace
    files:
    - ./config.json

namespace: banzai-web-frontend
