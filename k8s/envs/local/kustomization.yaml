apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./ns-banzai-web-frontend.yaml
  - ../../base/
  - ./ing-banzai-web-frontend/

configMapGenerator:
  - name: app-configuration
    behavior: replace
    files:
    - ./config.json

namespace: banzai-web-frontend
