apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./deploy-frontend.yaml
  - ./svc-frontend.yaml

configMapGenerator:
  - name: app-configuration
    files:
    - ./config.json
